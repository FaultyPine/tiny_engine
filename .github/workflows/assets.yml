name: assets-ci
on:
  pull_request:
  push:
    branches:
      - main
      - master

jobs:
  assets-package:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v3
    - uses: actions/upload-artifact@v3
    with:
      name: assets
      path: src/res/potp